
  # Create Homepage

  This is a code bundle for Create Homepage. The original project is available at https://www.figma.com/design/bHtLhLg4iPFCOYkucOm4yq/Create-Homepage.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  